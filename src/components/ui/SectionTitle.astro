---
const { sectionTitle, description } = Astro.props;
---

<div class="flex flex-col items-center justify-center gap-6 mb-10">
  <h2 class="greenhead text-center text-3xl sm:text-3xl font-medium p-4">{sectionTitle}</h2>
  {description &&<p class="w-auto text-center text-xl sm:w-[580px]">
    {description}
  </p>}
</div>
