---
title: Linux
description: خطوات إعداد خوادم DNS على توزيعات Linux مثل Ubuntu و Fedora و Debian
---

## إعداد الخدمة على نظام Linux

> اتبع الخطوات التالية لإعداد الخدمة.  
> يمكن أن تختلف الواجهة أو الأوامر قليلًا بين التوزيعات المختلفة.

---

### الطريقة الأولى: من خلال الواجهة الرسومية

1. افتح **الإعدادات** من قائمة النظام.  
2. اختر **الشبكة (Network)** من القائمة الجانبية.  
3. ضمن قسم **الاتصال السلكي (Wired)** أو **اللاسلكي (Wi-Fi)**، اضغط على **⚙️ إعدادات الاتصال**.  
4. انتقل إلى علامة التبويب **IPv4** أو **IPv6** حسب البروتوكول المستخدم.  
5. غيّر خيار **طريقة عنوان DNS** إلى **يدوي (Manual)**.  
6. في حقل **خوادم DNS**، أدخل **عناوين IP** الخاصة بخوادم DNS التي اخترتها، مفصولة بفاصلة.  
7. اضغط على **حفظ (Save)**، ثم أعد الاتصال بالشبكة لتطبيق الإعدادات الجديدة.

---

### الطريقة الثانية: من خلال سطر الأوامر (Terminal)

> هذه الطريقة مناسبة للمستخدمين ذوي الخبرة أو للأنظمة التي لا تحتوي على واجهة رسومية.

#### باستخدام NetworkManager (Ubuntu، Fedora، وغيرها)

1. افتح **الطرفية (Terminal)**.  
2. اعرض قائمة الاتصالات المتاحة:
   ```bash
   nmcli connection show
   ```
3. حدّد اسم الاتصال المستخدم (مثل `Wired connection 1` أو `Home Wi-Fi`).  
4. عيّن خوادم DNS المخصصة:
   ```bash
   sudo nmcli connection modify "اسم_الاتصال" ipv4.dns "34.1.57.14 34.1.60.230"
   ```
5. أعد تحميل الإعدادات أو أعد تشغيل الاتصال:
   ```bash
   sudo nmcli connection up "اسم_الاتصال"
   ```

#### باستخدام ‎/etc/resolv.conf‎ (في الأنظمة التقليدية)

1. افتح الملف كمستخدم جذر:
   ```bash
   sudo nano /etc/resolv.conf
   ```
2. أضف السطور التالية، مع استبدال العناوين بخوادم DNS التي تفضلها:
   ```bash
   nameserver 34.1.57.15
   nameserver 34.1.60.230
   ```
3. احفظ الملف وأغلقه.  
4. في بعض الأنظمة قد تحتاج إلى تحديث الخدمة لتطبيق التغييرات:
   ```bash
   sudo systemctl restart systemd-resolved
   ```

---

### التحقق من الإعداد

بعد تطبيق الإعدادات، يمكنك التأكد من أن النظام يستخدم خوادم DNS الجديدة عبر تنفيذ أحد الأوامر التالية:

```bash
dig example.com
```

أو:

```bash
systemd-resolve --status
```

---

> 💡 **ملاحظة**  
> قد تختلف الخطوات قليلًا من توزيعة لأخرى، لذا يُنصح بالرجوع إلى توثيق التوزيعة الخاصة بك في حال وجود اختلافات بسيطة.
