---
title: Android
description: خطوات تغيير خوادم DNS على أجهزة Android لاستخدام الخوادم 
---

## إعداد الخدمة على نظام Android

> الخطوات التالية تنطبق على معظم أجهزة Android الحديثة (Android 9 وما بعده).  
> قد تختلف بعض المسميات أو أماكن الإعدادات قليلًا حسب الشركة المصنعة للهاتف (Samsung، Huawei، Xiaomi...).

---

### الطريقة الأولى: من خلال إعدادات شبكة Wi-Fi

1. افتح **الإعدادات (Settings)** على جهازك.
2. اختر **الشبكة والإنترنت (Network & Internet)** أو **اتصالات (Connections)** حسب نوع الجهاز.
3. اضغط على **Wi-Fi**.
4. اختر الشبكة التي تتصل بها حاليًا، ثم اضغط مطولًا عليها أو اضغط على **رمز الإعداد ⚙️** بجانبها.
5. اختر **تعديل الشبكة (Modify Network)** أو **إدارة الشبكة (Manage network settings)**.
6. فعّل خيار **الإعدادات المتقدمة (Advanced options)**.
7. غيّر إعداد **إعدادات IP (IP settings)** من **DHCP** إلى **Static (ثابت)**.
8. مرّر للأسفل حتى تجد حقول **DNS 1** و **DNS 2**، وأدخل العناوين التالية:

   | النوع | العنوان |
   |--------|----------|
   | **DNS 1** | `34.1.57.15` |
   | **DNS 2** | `34.1.60.230` |

9. اضغط على **حفظ (Save)** لتطبيق الإعدادات.

---

### الطريقة الثانية: من خلال إعدادات Private DNS (في Android 9 وما بعده)

> هذه الطريقة تغيّر خوادم DNS على مستوى النظام بالكامل وليس فقط لشبكة معينة.

1. افتح **الإعدادات (Settings)**.  
2. اختر **الشبكة والإنترنت (Network & Internet)**.  
3. اضغط على **Advanced** ثم اختر **Private DNS**.
4. حدد الخيار **Private DNS provider hostname**.
5. أدخل اسم المضيف (hostname) الخاص بخدمة DNS في حال توفره.  
   > (إن كنت تستخدم عناوين IP فقط مثل أعلاه، استخدم الطريقة الأولى بدلًا من هذه).
6. اضغط على **Save** أو **حفظ**.

---

### التحقق من الإعداد

1. افتح المتصفح وانتقل إلى:
   ```
   https://www.sais.org.sa
   ```
   للتأكد من أن الاتصال لا يستخدم خوادم DNS خارجية.
2. يمكنك أيضًا استخدام تطبيق مثل **PingTools** أو **Network Info II** للتحقق من خوادم DNS الحالية.

---

> 💡 **ملاحظة:**  
> في بعض الهواتف (مثل Samsung One UI أو Xiaomi MIUI)، توجد إعدادات DNS داخل قسم **اتصال Wi-Fi** فقط،  
> وقد لا تظهر إعدادات “Private DNS” إلا في Android 9 فما فوق.
